#!/usr/bin/python

import sys
for line in sys.stdin:
	key = ""
	values = ""
	line = line.strip()
	splits = line.split("\t")
        question = splits[0]
	category = splits[1]
        question_words = question.split()

        for word in question_words:
                word = word.strip().lower()
		print word,'\t',1
 
